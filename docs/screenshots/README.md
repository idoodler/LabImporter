# Screenshots

Place README screenshots here using these exact file names (referenced from the
top-level `README.md`):

| File | Screen |
|---|---|
| `01-import.png` | Import landing (`ImportLandingView`) |
| `02-review.png` | Review & correct (`ReviewView`) |
| `03-dashboard.png` | Dashboard (`DashboardView`) |
| `04-trends.png` | Trend chart (`TrendsView`) |
| `05-history.png` | History list (`HistoryView`) |
| `06-settings.png` | Settings (`SettingsView`) |

Capture on a real Apple Intelligence device after importing the
[`../mock-data`](../mock-data) reports so every screen is populated. A consistent
device frame and appearance (e.g. iPhone 16 Pro, light mode) keeps the grid tidy.
